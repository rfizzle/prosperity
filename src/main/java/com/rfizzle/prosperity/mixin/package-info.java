/**
 * Mixins and accessors: instanced-loot unpack guards (container + minecart), mob loot-drop scaling,
 * container break protection, block-entity/minecart removal cleanup, and loot-table inspection
 * accessors (SPEC §1, §6, §10).
 */
package com.rfizzle.prosperity.mixin;
